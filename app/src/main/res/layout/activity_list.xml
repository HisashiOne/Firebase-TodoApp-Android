<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@drawable/main_background"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.hisashione.todoapp.Activities.ListActivity"
    >
    
    
    
    <EditText
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:hint="Item"
        android:gravity="center"
        android:id="@+id/itemText"
        android:background="@drawable/transparent_back"
        android:textColor="@android:color/white"
        />

    <Button android:layout_width="150dp"
        android:layout_height="40dp"
            android:background="@drawable/blue_btn"
            android:layout_toRightOf="@id/itemText"
            android:text="Fecha"
            android:textColor="@android:color/white"
            android:id="@+id/dateList"
            android:layout_marginLeft="10dp"
        />

    <Button android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/dateList"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="80dp"
            android:layout_marginRight="80dp"
            android:background="@drawable/blue_btn"
            android:text="GUARDAR"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:id="@+id/saveListBTN"
        />



    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/saveListBTN"
        android:layout_marginTop="20dp"
        android:id="@+id/itemList_1"
        android:background="@color/colorPrimary"
        />

</RelativeLayout>
